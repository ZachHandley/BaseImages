name: Build and Publish Base Images

on:
  push:
    branches:
      - main
    paths:
      - 'profiles/**'
      - 'scripts/**'
      - '.github/workflows/release-ubuntu.yaml'
  workflow_dispatch:
    inputs:
      profile:
        description: 'Profile to build (leave empty for all)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - base
          - rust
          - rust-containerd
          - ruby
          - python
          - go
          - ci-full
          - dotnet
          - cpp
          - java
          - db-clients
          - bun-ubuntu
          - rust-uv
          - flutter
          - rust-uv-flutter

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/zachhandley/baseimages

jobs:
  # Determine which profiles to build
  prepare:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Set build matrix
        id: set-matrix
        run: |
          if [[ "${{ github.event.inputs.profile }}" != "" && "${{ github.event.inputs.profile }}" != "base" ]]; then
            echo 'matrix={"profile":["${{ github.event.inputs.profile }}"]}' >> $GITHUB_OUTPUT
          else
            echo 'matrix={"profile":["rust","rust-containerd","ruby","python","go","ci-full","dotnet","cpp","java","db-clients","bun-ubuntu","rust-uv","flutter","rust-uv-flutter"]}' >> $GITHUB_OUTPUT
          fi

  build_base:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate date tag
        id: date
        run: echo "tag=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_BASE }}/base
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.date.outputs.tag }}
            type=sha,prefix=

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: profiles/base/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.IMAGE_BASE }}/base:latest
          artifact-name: sbom-base.spdx.json
          output-file: ./sbom-base.spdx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom-base
          path: ./sbom-base.spdx.json

  build_variants:
    needs:
      - prepare
      - build_base
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    if: ${{ github.event.inputs.profile != 'base' }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate date tag
        id: date
        run: echo "tag=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_BASE }}/${{ matrix.profile }}
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.date.outputs.tag }}
            type=sha,prefix=

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: profiles/${{ matrix.profile }}/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILDKIT_INLINE_CACHE=1

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: ${{ env.IMAGE_BASE }}/${{ matrix.profile }}:latest
          artifact-name: sbom-${{ matrix.profile }}.spdx.json
          output-file: ./sbom-${{ matrix.profile }}.spdx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@v4
        with:
          name: sbom-${{ matrix.profile }}
          path: ./sbom-${{ matrix.profile }}.spdx.json

  summary:
    needs:
      - build_base
      - build_variants
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Build Summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Profile | Image | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| base | \`ghcr.io/zachhandley/baseimages/base\` | ${{ needs.build_base.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| rust | \`ghcr.io/zachhandley/baseimages/rust\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| rust-containerd | \`ghcr.io/zachhandley/baseimages/rust-containerd\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ruby | \`ghcr.io/zachhandley/baseimages/ruby\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| python | \`ghcr.io/zachhandley/baseimages/python\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| go | \`ghcr.io/zachhandley/baseimages/go\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ci-full | \`ghcr.io/zachhandley/baseimages/ci-full\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| dotnet | \`ghcr.io/zachhandley/baseimages/dotnet\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| cpp | \`ghcr.io/zachhandley/baseimages/cpp\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| java | \`ghcr.io/zachhandley/baseimages/java\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| db-clients | \`ghcr.io/zachhandley/baseimages/db-clients\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| bun-ubuntu | \`ghcr.io/zachhandley/baseimages/bun-ubuntu\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| rust-uv | \`ghcr.io/zachhandley/baseimages/rust-uv\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| flutter | \`ghcr.io/zachhandley/baseimages/flutter\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| rust-uv-flutter | \`ghcr.io/zachhandley/baseimages/rust-uv-flutter\` | ${{ needs.build_variants.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Usage" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`yaml" >> $GITHUB_STEP_SUMMARY
          echo "jobs:" >> $GITHUB_STEP_SUMMARY
          echo "  build:" >> $GITHUB_STEP_SUMMARY
          echo "    runs-on: ubuntu-latest" >> $GITHUB_STEP_SUMMARY
          echo "    container:" >> $GITHUB_STEP_SUMMARY
          echo "      image: ghcr.io/zachhandley/baseimages/ci-full:latest" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
